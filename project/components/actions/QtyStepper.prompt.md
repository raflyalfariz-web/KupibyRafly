Minus / count / plus, with 48px square taps — the only quantity control in the system.

```jsx
<QtyStepper value={qty} label="500ml" onChange={setQty} />
```

The count is Zilla Slab 600, tabular, at price size so it reads at arm's length. Disabled-looking minus at `min` is opacity only — the tap target stays.
