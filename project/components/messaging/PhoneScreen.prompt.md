The mock frame. 390×780 — a mid-range Android, which is what customers actually hold.

```jsx
<PhoneScreen chrome="whatsapp" title="KUPI by Rafly" subtitle="online">
  <ChatBubble from="customer">…</ChatBubble>
</PhoneScreen>
```

`chrome="browser"` gives the brown status bar and KUPI page surface for the QR landing mock; it drops the composer and the padding so a full page can sit inside.
