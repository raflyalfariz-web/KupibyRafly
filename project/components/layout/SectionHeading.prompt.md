Every section on every KUPI surface starts with this.

```jsx
<SectionHeading eyebrow="Menu hari ini" sub="Diseduh subuh, dikirim siang.">Kopi harian</SectionHeading>
```

Eyebrow is amber uppercase 12px; title is Zilla Slab 600 at 32px; the 48×3px brown rule closes it. Left-aligned by default — centre only on the QR landing hero.
