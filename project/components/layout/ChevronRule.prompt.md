Three small joglo marks as a section divider — the only ornament in the system.

```jsx
<ChevronRule base="../../assets" count={3} width={24} />
```

Use once per screen, between a thank-you and a footer. Never as a bullet, never inside a card.
